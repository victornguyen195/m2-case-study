/**
 * Visible state of shipping address form for custom display instead of popup
 */
define([
    'ko'
], function (ko) {
    'use strict';

    return {
        isVisible: ko.observable(false)
    };
});
